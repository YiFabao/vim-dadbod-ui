当前项目需要改一下，saved queries: 下要用文夹件目录结构树，可以展开也可以收缩，当前改的不支持展开，而是在新的界面打开了目录详情
如下:
▾ 󰆼 climb ✓
    󰓰 New query
  ▾  Saved queries (8)
       connection
       miner
       ops
       report
       sni
       test
       traffic
       user


帮我先修复这个展开收缩的功能，展开后具体的缓存我是可以跟原一样，打开这个SQL文件然后执行查询的

然后再加上可以添加目录的功能，类似于添加文件夹的功能，添加文件的功能,也可以删除
